# Interactions work package

Baseline: local/GitHub HEAD `ec817f41db75d9b422b3a5d1c480a9e631b295ab`, tag `release-20260926-205506`; local, GitHub, online `admin.html`, and 27 CRM Functions (113 source/config files) matched. `public.interactions` did not exist. Live schema confirmed `persons.legacy_customer_id`, `followups.customer_id`, `recruit_followups.candidate_id` through `recruit_candidates.customer_id`, and polymorphic activity participation. The source tables were not altered.

Migration version `20260926213135` was planned, dry-run classified as a security change, and applied successfully to the production CloudBase migration history. Post-migration schema readback found 13 columns, two foreign keys, the person/time and source uniqueness indexes, forced RLS, the service-only policy, and zero ledger rows. The table ACL lists only `service_role` and its owner. Rollback is data guarded.

Compatibility design: read-through of bounded active legacy records; no backfill and no changes to legacy CRUD, existing routes, or Quick Capture. Only explicit manual capture writes the new ledger. The service rejects absent Persons, invalid values, ambiguous identity mapping, and date-only manual timestamps. Production write tests require a separately marked test Person and are not run against real customer records.

The isolated `InteractionService` suite passed 6/6 cases: all four sources, attendance-only activity mapping, conflicting speaker exclusion, materialized-source deduplication, standalone Person behavior, manual capture validation/audit, and bounded results. The full offline CRM regression passed 60, failed 0, skipped 5. `npm run check:shared`, syntax, and `git diff --check` passed. The new service is source code only at this stage; no existing Cloud Function or frontend route has been changed. Therefore its production HTTP path and manual database write path remain unverified.

The production migration was applied before the foundation Git publication because the new table was explicitly requested. The foundation commit is `a902fda8daadf0a8d155f0c79c613b2f437336ef`, tag `release-20260926-221511`. That release's first post-release cloud check failed only when the local temporary audit drive filled (`ENOSPC`); after removing verified generated audit folders, a full rerun passed for local, GitHub, online page, and 27 functions. No source mismatch was found.

The user then explicitly approved adding two actions to the existing `person_360` function after the compatibility impact and regression scope were explained. This integration adds `listInteractions` and `createInteraction`, reusing the existing real-login guard and server-side PG credential. Legacy source tables are restricted to GET in the function adapter. No existing action, frontend route, or legacy writer changed. The `interaction-service.js` copy in `person_360` must match the `_shared` source hash on every deployment; the existing shared sync script still governs only db.js and ai.js.

Integration tests passed 11/11 for service, dispatch and shared/deployed copy equality; existing household tests passed 5/5. The full offline CRM regression passed 60, failed 0, skipped 5 after deployment. Only `person_360` code was deployed; pre-release cloud source verification found all 27 functions and 114 source/config files matching local. A real unauthenticated invocation of `listInteractions` returned `UNAUTHORIZED`. After a test-account login in an isolated browser, the deployed function returned an existing Person, one bounded timeline row from a legacy source and no error; an invalid `createInteraction` request was rejected before database write. The probe saved only `{hasPerson, timelineRows, hasLegacy, invalidWriteRejected, error}` and no Person name, raw note, token or password. No production interaction write has been executed because no explicitly marked test Person exists. The manual creation success path is covered by isolated tests, not by a production write.
