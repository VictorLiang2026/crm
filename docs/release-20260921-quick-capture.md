# 快速录入防误关闭修复

- 基线与回滚版本：`e41f50d7033893e0ccd2cd3d5a09417288e85753` / `release-20260920-075851`。
- 本轮发布标签：`release-20260921-110000`。
- 变更：仅移除 `admin.html` 中 `openQuickCapture` 的背景点击关闭监听。未保存文字、AI 处理中状态以及解析后手工调整均不会因点击窗口外而消失。
- 保留行为：点击“取消”主动关闭；点击“确认并保存”且保存成功后关闭；保存失败时继续保留窗口。其他弹窗逻辑不变。
- 影响：顶部快速记录、客户详情与增员详情的快速录入入口。接口、字段、数据库、权限、登录及既有保存流程不变。未访问 `pr` schema 或调用、修改、部署 `pr_` 函数。
- 基线检查：GitHub master / 标签、本地与线上页面一致；26 个 CRM 云函数的 108 个源码/配置文件全部一致。
- 浏览器验证：使用 Edge 无头浏览器执行真实源码中的 DOM 辅助函数与快速录入函数，后端替换为虚拟响应，不连接生产数据库。修复前复现填写后点击背景弹窗消失；修复后验证输入中、AI 等待中、AI 结果出现后点击背景均保留内容，修改的日期保留，保存失败保留窗口，确认保存成功关闭且提交日期正确，三个入口均可主动取消。全部通过，无页面异常。所有内联 JavaScript 语法检查通过。
- 发布范围：仅上传 `admin.html` 至现有静态托管 `/crm/admin.html`；不部署云函数。使用 `tools/release.ps1` 完成提交、推送、标签及提交前/发布后全量源码核对，结果以发布输出为准。
- 发布后交互复核：下载线上实际 `admin.html`，再次执行上述隔离浏览器回归，全部通过；线上与本地页面 SHA-256 一致。
- 托管预检：现有 URL 返回 HTTP 200、`Content-Type: text/html`、禁止缓存；原有 `Content-Disposition: attachment` 本轮不调整。
- 未验证项：未使用真实账号进行端到端 AI 解析或生产写入，未重新执行所有其他模块业务操作；源码一致性不代表业务回归。页面刷新、浏览器关闭后的草稿恢复不属于本轮修复范围。
- 回滚：如需要，取上述基线的 `admin.html` 恢复发布并创建恢复提交和新标签；不强推，不回滚业务数据。
