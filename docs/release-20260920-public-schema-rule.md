# public schema 隔离规则

- 基线：`07f5939` / `release-20260920-063556`。
- 本轮标签：`release-20260920-075851`。
- 变更：在 AGENTS.md、开发环境文档、开发安全边界说明中同步用户新增的 public-only 数据范围和 pr / pr_ 隔离约定。
- 影响：仅文档规则，不修改业务代码、数据库结构/数据或云端资源；无需覆盖线上业务产物。
- 验证：文档差异检查；发布脚本提交前和推送后核对线上页面与 26 个 CRM 函数源码，并验证 GitHub master/标签一致。执行结果见本轮任务输出。
- 未执行：登录后的业务回归、数据库查询或迁移；没有访问 pr schema 或调用 pr_ 函数。
- 恢复：若后续需要调整规则，先取得明确授权，再创建规则变更提交；不对业务数据或云函数执行恢复操作。
