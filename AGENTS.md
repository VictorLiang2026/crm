# CRM 开发约定

用户于 2026-09-20 确认，以下规则适用于后续所有迭代。

## 不可违反的工作规则（2026-09-24）

以下规则为强制约束；如与便利性、临时实现或旧工作习惯冲突，以本节为准。

1. 当前主系统只使用 `public` schema。
2. 不访问、修改或依赖 `pr` schema、`pr_*` 对象或 `pr_*` Cloud Functions。
3. 不删除或重命名已有表、Views、Cloud Functions、Routes，除非用户针对该项单独批准。
4. 任何新的数据库变更必须同时提供 migration 与 rollback。
5. 新 AI 功能不得硬编码具体模型厂商。
6. 模型选择必须交给 AI Gateway 或 CloudBase 配置。
7. 重要 AI 结果必须保留 human-in-the-loop，由人确认后再产生关键业务影响。
8. 任何跨模块改动必须执行 regression，并记录结果。
9. Legacy 功能优先保留；除非需求明确要求，不移除、不替换现有兼容行为。
10. 每个工作包只完成用户指定范围。
11. 不允许“大顺手重构”；发现额外问题只记录和报告，不借当前工作包扩张修改面。
12. 遇到高风险操作必须先停止并报告风险、影响范围和所需授权，不得自行继续。
13. 从现在起，新 AI-native 前端功能必须放在 `crm/js/modules/`，复用 `crm/js/core/`、`crm/js/components/` 与 `crm/css/`；保留 `admin.html` 的旧实现，不为建立新功能而整体抽离旧代码。

## 环境与范围

- 本地：`D:\CRM\crm`；GitHub：`https://github.com/VictorLiang2026/crm`；发布分支：`master`。
- 生产 CloudBase：`crm-d1gkae8ddc930d151`；静态页面：`https://crm-d1gkae8ddc930d151-1434199662.tcloudbaseapp.com/crm/admin.html`。
- **当前系统所有迭代升级的数据操作仅限 `public` schema**，包括查询、写入、迁移、表/视图/数据库函数、权限调整及测试数据操作。SQL 明确限定 `public`，避免依赖不确定的 `search_path`。
- **不涉及 `pr` schema，也不涉及任何以 `pr_` 开头的云函数**：不查询或读写 `pr` 数据，不修改其结构/权限，不调用、修改、部署或删除 `pr_` 函数；不得通过跨 schema 引用或级联操作间接影响它们。发现依赖时停止相关操作并报告，不扩大本轮范围。
- CRM 使用无前缀云函数。不要操作用途未确认的体验版环境。
- 先读 `docs/development-environment.md`、`docs/开发安全边界说明.md`。旧文档与实时查询冲突时，以核实过的线上事实为准。

## 修改前

1. 检查本地、GitHub 和云端基线，记录当前提交与回滚版本；发现差异先查清原因，禁止直接覆盖。
2. 说明影响的页面、接口、字段、数据、权限及现有流程。
3. **如果新实现可能改变或影响已有功能，必须在写相关代码前说明影响、兼容方案和回归范围，获得用户明确确认后才能开发。** 不得以一般开发授权代替这种确认。
4. 无已有功能行为影响的修复、文档和工具完善，可以在已授权范围内继续。

## 修改与验证

- 保持 `admin.html` 现有单文件业务实现、callFn 数据入口、现有登录/RLS/软删除与恢复语义；新 AI-native 前端功能遵守上面的模块化例外，不擅自升级运行时或重构旧业务。
- 云函数共享模块有独立副本，修改后同步受影响副本并逐个部署。基表改列必须检查并重建依赖视图。
- 按实际影响执行语法检查和业务回归，覆盖修改功能及相邻旧功能；涉及公共入口时检查登录、客户列表/详情、跟进、活动、增员、回收站。
- 不拿真实客户数据做破坏性测试。业务验证未执行或失败必须明确报告，不能仅凭哈希一致声称功能正常。

## 每轮必须完成发布

- 用户已持续授权：每轮修改完成后，部署本轮需要发布的产物，提交并推送 GitHub，打发布标签，核对本地/云端/GitHub 一致。不必每次再询问是否提交或发布。
- 仅部署明确的页面文件及受影响 CRM 函数，禁止上传整个项目目录或盲目全量部署。配置清单不是授权修改所有云函数的指令。
- 文档/工具修改不需覆盖未改变的业务文件，但仍须完成 Git 发布、标签及云端一致性验证，在发布记录说明云端产物未改变。
- 发布执行顺序与命令见环境文档；部署失败、Git 失败或回归失败立即停止收尾，不得报告成功。不得强推、静默回滚用户数据或绕过审批限制。
- 发布后报告提交、标签、线上地址、部署范围、验证证据及未验证项。故障时先恢复可用性；数据库回滚另行评估数据影响。
